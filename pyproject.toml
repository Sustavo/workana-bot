[project]
name = "workana-screp"
version = "0.1.0"
description = "Semi-automated proposal submitter for Workana"
requires-python = ">=3.11"

[tool.ruff]
line-length = 110
target-version = "py311"

[tool.ruff.lint]
select = ["E", "F", "I", "B", "UP"]
